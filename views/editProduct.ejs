<%- include('./partials/headerOwner') %>
    

            <div class="min-h-screen flex flex-col">
                <div class="container px-10 py-20 flex flex-grow">

                    <main class="w-3/4 bg-white p-8 shadow ml-4">
                        <h2 class="text-xl font-bold mb-4">Update Product Details</h2>
                        <form autocomplete="off" action="/products/edit/<%= product._id %>" method="post">
                            <div class="mb-6">
                                <h3 class="text-lg font-semibold mb-2">Product Details</h3>
                                <div class="grid grid-cols-2 gap-4">
                                    <input name="name" type="text" placeholder="Product Name" value="<%= product.name %>"
                                        class="border p-2 rounded w-full">
                                    <input name="price" type="text" placeholder="Product Price" value="<%= product.price %>"
                                        class="border p-2 rounded w-full">
                                    <input name="discount" type="text" placeholder="Discount Price" value="<%= product.discount %>"
                                        class="border p-2 rounded w-full">
                                </div>
                            </div>

                            <div>
                                <h3 class="text-lg font-semibold mb-2">Panel Details</h3>
                                <div class="grid grid-cols-2 gap-4">
                                    <input name="bgcolor" type="text" placeholder="Background Color" value="<%= product.bgcolor %>"
                                        class="border p-2 rounded w-full">
                                    <input name="panelcolor" type="text" placeholder="Panel Color" value="<%= product.panelcolor %>"
                                        class="border p-2 rounded w-full">
                                    <input name="textcolor" type="text" placeholder="Text Color" value="<%= product.textcolor %>"
                                        class="border p-2 rounded w-full">
                                </div>
                            </div>
                            <input class="px-5 py-2 rounded-full mt-3 bg-yellow-600 text-white" type="submit"
                                value="Update Product">
                        </form>
                    </main>
                </div>
            </div>
            <%- include('./partials/footer') %>